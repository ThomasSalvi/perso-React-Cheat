import React from 'react';

const Item13 = () => (
  <div className="items">
    <h1 className="items-title">array.find()</h1>
    <p>Retourne un tableau contenant les valeurs répondant à la condition souhaitée</p>
    <pre>
      <code className="language-jsx">
  {`
    const App = ({data}) => {
      const finder = () => {
      return (
        let found = data.find((element) => (
          element.id > 10;
        );
      }
  `}
      </code>
    </pre>
  </div>
);

export default Item13;
